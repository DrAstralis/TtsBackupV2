<Window x:Class="TtsBackup.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TtsBackup.Wpf"
        xmlns:vm="clr-namespace:TtsBackup.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="TTS Asset Backup" Height="600" Width="1000">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <!-- Pass 1: empty shell; we’ll build out tree / panels in later passes -->
        <TextBlock Text="TTS Backup Tool (shell)"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="24"
                   FontWeight="Bold"/>
    </Grid>
</Window>
